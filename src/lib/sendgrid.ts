import type { Customer, Mode } from "./types";

async function sendEmail(mode: Mode, customer: Customer) {
    // Sendgrid code
}

async function fetchEmailTemplate(mode: Mode) {
    // sendgrid code
}

export default{}